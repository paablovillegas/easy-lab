
const initialState = {
    ordenes: [],
    active: null,
}

export const ordenReducer = (state = initialState, { type, payload }) => {
    switch (type) {
        default:
            return state;
    }
}