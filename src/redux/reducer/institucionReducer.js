
const initialState = {
    instituciones: [],
    active: null,
}

export const institucionReducer = (state = initialState, { type, payload }) => {
    switch (type) {
        default:
            return state;
    }
}